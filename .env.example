# Optional SecretSpec runtime dotenv provider. This template has no credentials.
# Copy to .env locally, restrict permissions, and enter your own values there.
BLOCKSTREAM_CLIENT_ID=
BLOCKSTREAM_CLIENT_SECRET=
MIRO_ACCESS_TOKEN=
