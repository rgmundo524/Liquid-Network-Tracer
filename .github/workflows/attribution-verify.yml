name: Validate attribution limits and address display
on:
  push:
    branches: [codex/attribution-hop-limits]
    paths:
      - .github/workflows/attribution-verify.yml
      - .github/attribution.part*.b64
permissions:
  contents: write
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '24.19.0'
      - name: Apply exact-source checked changes
        run: |
          python3 - <<'PY'
          import base64, hashlib, json, lzma
          from pathlib import Path
          parts = sorted(Path('.github').glob('attribution.part*.b64'))
          assert [p.name for p in parts] == ['attribution.part0.b64', 'attribution.part1.b64', 'attribution.part2.b64']
          raw = lzma.decompress(base64.b64decode(''.join(p.read_text().strip() for p in parts), validate=True))
          assert hashlib.sha256(raw).hexdigest() == 'd220c9963b96df758fc95b78eefcf20e1c1b5f3d345eaa80b976a2488dd31495'
          prepared = {}
          for change in json.loads(raw):
              path = Path(change['path'])
              assert not path.is_absolute() and '..' not in path.parts
              assert path.parts[0] in ('liquid_tracer', 'tests', 'docs', 'web')
              assert path not in prepared and not path.is_symlink()
              original = path.read_bytes() if path.exists() else b''
              assert hashlib.sha256(original).hexdigest() == change['before'], str(path)
              text = original.decode('utf-8')
              end = len(text)
              for start, stop, replacement in reversed(change['ops']):
                  assert type(start) is int and type(stop) is int and 0 <= start <= stop <= end
                  text = text[:start] + replacement + text[stop:]
                  end = start
              result = text.encode('utf-8')
              assert hashlib.sha256(result).hexdigest() == change['after'], str(path)
              prepared[path] = result
          for path, result in prepared.items():
              path.parent.mkdir(parents=True, exist_ok=True)
              path.write_bytes(result)
          PY
          git rm .github/attribution.part*.b64 .github/workflows/attribution-source.yml .github/workflows/attribution-verify.yml
          git add liquid_tracer tests docs web
          git diff --cached --check
      - name: Install test dependencies
        run: |
          python3 -m venv .venv
          .venv/bin/python -m pip install '.[tui]'
          npm --prefix layout ci --ignore-scripts --no-audit --no-fund
      - name: Verify hop limits and native address count labels
        run: .venv/bin/python -m unittest tests.test_attribution_hop_limits tests.test_address_count_labels tests.test_address_import tests.test_web_addresses tests.test_services_ui -v
      - name: Check and build browser interface
        run: |
          npm --prefix web ci --ignore-scripts --no-audit --no-fund
          npm --prefix web run check
          npm --prefix web run build
      - name: Run full offline regression suite
        run: .venv/bin/python -m unittest discover -s tests -v
      - name: Publish verified source to feature branch
        run: |
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Add per-attribution hop limits and address count labels'
          git push origin HEAD:refs/heads/codex/attribution-hop-limits
